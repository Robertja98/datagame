// Auto-generated answer key for all open-ended questions in the game
// Format: { id, prompt, answer }
import { questions } from './questions';

export const answerKey = questions.map(({ id, prompt, answer }) => ({ id, prompt, answer }));
